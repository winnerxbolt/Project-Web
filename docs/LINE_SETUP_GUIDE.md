# LINE Notification Setup Guide 💬

คู่มือการตั้งค่าและใช้งาน LINE Notification System

## 📋 สารบัญ

1. [ภาพรวมระบบ](#ภาพรวมระบบ)
2. [การสร้าง LINE Official Account](#การสร้าง-line-official-account)
3. [การตั้งค่า Webhook](#การตั้งค่า-webhook)
4. [การเชื่อมต่อกับระบบ](#การเชื่อมต่อกับระบบ)
5. [การทดสอบ](#การทดสอบ)
6. [การใช้งาน](#การใช้งาน)
7. [Troubleshooting](#troubleshooting)

---

## 🎯 ภาพรวมระบบ

LINE Notification System ช่วยให้คุณสามารถ:
- ✅ ส่งการแจ้งเตือนการจองผ่าน LINE
- ✅ ส่งการแจ้งเตือนก่อนเช็คอิน/เช็คเอาท์
- ✅ ส่งโปรโมชั่นแบบ Broadcast ถึงทุกคน
- ✅ ตอบกลับอัตโนมัติเมื่อมีข้อความเข้ามา
- ✅ ติดตามประวัติการส่งข้อความ

### คุณสมบัติหลัก

| คุณสมบัติ | รายละเอียด |
|-----------|-----------|
| **Webhook** | รับ Event จาก LINE (message, follow, unfollow) |
| **Push Message** | ส่งข้อความถึงผู้ใช้โดยตรง |
| **Flex Message** | ส่งข้อความแบบ Rich Format |
| **Auto Reply** | ตอบกลับอัตโนมัติด้วย Keyword |
| **Broadcast** | ส่งข้อความถึงทุกคน |

---

## 🚀 การสร้าง LINE Official Account

### Step 1: สร้าง LINE Official Account

1. ไปที่ [LINE Official Account Manager](https://manager.line.biz/)
2. เข้าสู่ระบบด้วยบัญชี LINE ของคุณ
3. คลิก **"สร้างบัญชีใหม่"**
4. กรอกข้อมูล:
   - **ชื่อบัญชี**: WinnerBoy Pool Villa
   - **ประเภทธุรกิจ**: โรงแรม/ที่พัก
   - **คำอธิบาย**: บ้านพักพูลวิลล่าสุดหรู
5. อ่านและยอมรับข้อกำหนด
6. คลิก **"สร้างบัญชี"**

### Step 2: เข้าสู่ LINE Developers Console

1. ไปที่ [LINE Developers Console](https://developers.line.biz/)
2. เข้าสู่ระบบด้วยบัญชีเดียวกับที่สร้าง Official Account
3. คลิก **"Create a new provider"** (ถ้ายังไม่มี Provider)
   - **Provider name**: WinnerBoy
4. คลิก **"Create a Messaging API channel"**
5. กรอกข้อมูล Channel:
   - **Channel name**: WinnerBoy Booking System
   - **Channel description**: ระบบแจ้งเตือนการจองและโปรโมชั่น
   - **Category**: Hotels and Tourism
   - **Subcategory**: Hotel/Resort
6. อ่านและยอมรับข้อกำหนด
7. คลิก **"Create"**

### Step 3: ดึง Credentials

หลังจากสร้าง Channel แล้ว:

1. ไปที่แท็บ **"Basic settings"**
2. คัดลอก **Channel Secret**
3. ไปที่แท็บ **"Messaging API"**
4. ที่ส่วน **"Channel access token"**:
   - คลิก **"Issue"** (ถ้ายังไม่มี Token)
   - คัดลอก **Channel Access Token**

⚠️ **สำคัญ**: เก็บ Token และ Secret ไว้ปลอดภัย อย่าแชร์กับใคร!

---

## 🔗 การตั้งค่า Webhook

### ขั้นตอนการตั้งค่า Webhook URL

1. กลับไปที่ **LINE Developers Console**
2. ไปที่ Channel ที่สร้างไว้
3. คลิกแท็บ **"Messaging API"**
4. ที่ส่วน **"Webhook settings"**:

#### สำหรับ Production:
```
https://your-domain.com/api/line/webhook
```

#### สำหรับ Development (ใช้ ngrok):

1. ติดตั้ง ngrok:
```bash
# Download from https://ngrok.com/download
# หรือใช้ Chocolatey (Windows)
choco install ngrok
```

2. รัน Development Server:
```bash
npm run dev
```

3. สร้าง Tunnel ด้วย ngrok:
```bash
ngrok http 3000
```

4. คัดลอก HTTPS URL ที่ได้:
```
https://abc123.ngrok.io/api/line/webhook
```

5. วาง URL ใน LINE Developers Console

6. คลิก **"Update"**

7. เปิด **"Use webhook"** → เลื่อน Toggle เป็น **Enable**

8. คลิก **"Verify"** เพื่อทดสอบ Webhook

### การปิดการตอบกลับอัตโนมัติของ LINE

เพื่อให้ระบบของเราตอบกลับแทน:

1. ใน LINE Developers Console
2. แท็บ **"Messaging API"**
3. ที่ส่วน **"LINE Official Account features"**:
   - **Auto-reply messages**: ปิด (Disabled)
   - **Greeting messages**: ปิด (Disabled) หรือตั้งค่าเอง

---

## ⚙️ การเชื่อมต่อกับระบบ

### 1. เข้าสู่ Admin Panel

```
https://your-domain.com/admin
```

### 2. คลิกปุ่ม "💬 LINE Notification"

### 3. ในหน้า LINE Settings:

#### a) เปิดใช้งาน LINE:
- เลื่อน Toggle **"เปิดใช้งาน LINE Notification"** → **ON**

#### b) กรอก Credentials:
- **Channel Access Token**: วาง Token ที่คัดลอกมา
- **Channel Secret**: วาง Secret ที่คัดลอกมา

#### c) คัดลอก Webhook URL:
- คลิกปุ่ม **"คัดลอก"** ข้างหลัง Webhook URL
- นำไปวางใน LINE Developers Console

#### d) เลือกประเภทการแจ้งเตือน:
ติ๊กเลือกประเภทที่ต้องการ:
- ✅ **การจอง** - ส่งเมื่อมีการจองสำเร็จ
- ✅ **การชำระเงิน** - ส่งเมื่อชำระเงินสำเร็จ
- ✅ **เช็คอิน** - แจ้งเตือนก่อนเช็คอิน 24 ชม.
- ✅ **เช็คเอาท์** - แจ้งเตือนในวันเช็คเอาท์
- ✅ **โปรโมชั่น** - ส่งโปรโมชั่นพิเศษ
- ✅ **การแจ้งเตือน** - การแจ้งเตือนทั่วไป

#### e) เปิดใช้งาน Auto Reply:
- เลื่อน Toggle **"Auto Reply"** → **ON**

### 4. บันทึกและทดสอบ

- คลิกปุ่ม **"บันทึกการตั้งค่า"**
- คลิกปุ่ม **"ทดสอบการเชื่อมต่อ"**
- ถ้าสำเร็จจะแสดง ✅ **"การเชื่อมต่อ LINE สำเร็จ!"**

---

## 🧪 การทดสอบ

### 1. ทดสอบการเพิ่มเพื่อน

1. ใน LINE Developers Console
2. แท็บ **"Messaging API"**
3. หา **QR Code** หรือ **LINE ID**
4. เพิ่มบอทเป็นเพื่อนด้วยมือถือ
5. ควรได้รับข้อความต้อนรับพร้อม Flex Message

### 2. ทดสอบ Auto Reply

ส่งข้อความไปที่บอท:
- **"จอง"** หรือ **"book"** → ข้อมูลการจอง
- **"ราคา"** หรือ **"price"** → ราคาห้องพัก
- **"สถานที่"** หรือ **"location"** → ที่อยู่พร้อมแผนที่
- **"ติดต่อ"** หรือ **"contact"** → ช่องทางติดต่อ
- ข้อความอื่นๆ → เมนูช่วยเหลือ

### 3. ทดสอบส่งข้อความ

ใน Admin Panel → แท็บ **"ส่งข้อความ"**:
1. พิมพ์ข้อความทดสอบ
2. คลิก **"ส่งข้อความ"**
3. ตรวจสอบว่าได้รับใน LINE

### 4. ทดสอบการจองจริง

1. ทำการจองห้องพักผ่านเว็บไซต์
2. เสร็จสิ้นการชำระเงิน
3. ตรวจสอบว่าได้รับการแจ้งเตือนใน LINE พร้อม:
   - 📋 รายละเอียดการจอง
   - 🎫 ปุ่มดาวน์โหลด E-Ticket
   - 🎁 คะแนน Loyalty

---

## 💡 การใช้งาน

### ส่งข้อความถึงผู้ใช้

#### 1. ผ่าน Admin Panel:

```
Admin → LINE Notification → แท็บ "ส่งข้อความ"
```

- พิมพ์ข้อความ
- คลิก "ส่งข้อความ"
- ข้อความจะถูกส่งถึงทุกคนที่เพิ่มบอทเป็นเพื่อน

#### 2. ผ่าน API:

```typescript
// ส่งข้อความถึงผู้ใช้เดียว
await fetch('/api/line/send', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    lineUserId: 'Uxxxxx',
    type: 'text',
    message: 'สวัสดีค่ะ'
  })
})

// Broadcast ถึงทุกคน
await fetch('/api/line/send', {
  method: 'PUT',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    message: '🎁 โปรโมชั่นพิเศษวันนี้!',
    type: 'text'
  })
})
```

### ส่ง Flex Message

```typescript
import { bookingConfirmation } from '@/lib/lineTemplates'

const flexMessage = bookingConfirmation({
  id: 'BK001',
  roomName: 'Pool Villa Deluxe',
  guestName: 'คุณสมชาย',
  checkIn: '15/01/2025',
  checkOut: '17/01/2025',
  nights: 2,
  total: 20000
})

await fetch('/api/line/send', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    lineUserId: 'Uxxxxx',
    type: 'flex',
    flexMessage
  })
})
```

### ดูประวัติข้อความ

```
Admin → LINE Notification → แท็บ "ประวัติ"
```

- แสดงข้อความทั้งหมดที่ส่งไป
- สถานะ: ✅ ส่งสำเร็จ / ❌ ล้มเหลว
- วันเวลาที่ส่ง

### ดูรายชื่อผู้ใช้

```
Admin → LINE Notification → แท็บ "ผู้ใช้"
```

- แสดงผู้ใช้ทั้งหมดที่เพิ่มบอทเป็นเพื่อน
- รูปโปรไฟล์ + ชื่อ
- สถานะข้อความ
- วันที่เพิ่มเพื่อน

---

## 🔧 Troubleshooting

### ปัญหา: Webhook ไม่ทำงาน

**สาเหตุ**:
- URL ไม่ใช่ HTTPS
- Server ไม่ได้เปิด
- Signature verification ล้มเหลว

**วิธีแก้**:
1. ตรวจสอบว่าใช้ HTTPS (ngrok สำหรับ dev)
2. ตรวจสอบ Server Status
3. ตรวจสอบ Channel Secret ถูกต้อง
4. ดู Log ใน Console:
```bash
npm run dev
# ดู error logs
```

### ปัญหา: ส่งข้อความไม่ได้

**สาเหตุ**:
- Token หมดอายุหรือไม่ถูกต้อง
- lineUserId ไม่ถูกต้อง
- ผู้ใช้บล็อกบอท

**วิธีแก้**:
1. ตรวจสอบ Channel Access Token
2. ทดสอบการเชื่อมต่อใน Admin Panel
3. Issue Token ใหม่ถ้าจำเป็น
4. ตรวจสอบ lineUserId ใน `data/line-users.json`

### ปัญหา: Auto Reply ไม่ทำงาน

**สาเหตุ**:
- Auto Reply ปิดใน Settings
- Auto-reply messages เปิดใน LINE Console

**วิธีแก้**:
1. เปิด Auto Reply ใน Admin Panel
2. ปิด Auto-reply messages ใน LINE Console
3. Verify Webhook ใหม่

### ปัญหา: ไม่ได้รับข้อความต้อนรับ

**สาเหตุ**:
- Webhook ไม่ทำงาน
- Greeting messages เปิดใน LINE Console

**วิธีแก้**:
1. ตรวจสอบ Webhook URL
2. ปิด Greeting messages ใน LINE Console
3. ลองเพิ่มเพื่อนใหม่

### ปัญหา: Flex Message แสดงผลไม่ถูกต้อง

**สาเหตุ**:
- JSON structure ไม่ถูกต้อง
- รูปภาพ URL ไม่ใช่ HTTPS

**วิธีแก้**:
1. ตรวจสอบ JSON structure ใน `lib/lineTemplates.ts`
2. ใช้ [LINE Flex Message Simulator](https://developers.line.biz/flex-simulator/) ทดสอบ
3. แก้ไข URL รูปภาพเป็น HTTPS

---

## 📊 Rate Limits

LINE มีข้อจำกัดในการส่งข้อความ:

| Plan | Push Messages/Month | Broadcast |
|------|---------------------|-----------|
| **Free** | 500 | ไม่จำกัด (แต่ต้องมีผู้ติดตาม) |
| **Light** | 5,000 | ไม่จำกัด |
| **Standard** | 45,000 | ไม่จำกัด |

**หมายเหตุ**: ระบบมีการจำกัดอัตราการส่งแบบ Broadcast ที่ 100ms ต่อข้อความเพื่อป้องกันการถูกบล็อก

---

## 🔐 Security

### การเก็บรักษา Credentials

- ❌ **ห้าม** Commit Token/Secret ลง Git
- ✅ ใช้ Environment Variables:

```env
LINE_CHANNEL_ACCESS_TOKEN=your_token_here
LINE_CHANNEL_SECRET=your_secret_here
```

### Signature Verification

ระบบตรวจสอบ Signature ของ Webhook ทุกครั้ง:
```typescript
// lib/lineTemplates.ts
const signature = crypto
  .createHmac('SHA256', channelSecret)
  .update(body)
  .digest('base64')

if (signature !== xLineSignature) {
  throw new Error('Invalid signature')
}
```

---

## 📚 Resources

- [LINE Messaging API Documentation](https://developers.line.biz/en/docs/messaging-api/)
- [Flex Message Simulator](https://developers.line.biz/flex-simulator/)
- [LINE Official Account Manager](https://manager.line.biz/)
- [LINE Developers Console](https://developers.line.biz/console/)

---

## 🆘 Support

หากพบปัญหาหรือต้องการความช่วยเหลือ:

1. ตรวจสอบ [Troubleshooting](#troubleshooting)
2. ดู API Logs ใน Console
3. ทดสอบด้วย LINE Flex Message Simulator
4. ติดต่อทีมพัฒนา

---

**สร้างโดย**: WinnerBoy Development Team  
**อัพเดทล่าสุด**: มกราคม 2025  
**เวอร์ชั่น**: 1.0.0
